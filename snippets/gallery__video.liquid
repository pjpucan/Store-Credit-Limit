{% comment %}
  Video Gallery Snippet for Shopify Themes.
  This snippet is used to render a video with customizable settings and controls, ensuring it fits well within the theme's design and layout preferences.

  Accepts:
  - media: {Object} Liquid object for video media.
  - ratio_class: {string} Class string to set the aspect ratio of the video container.
  - crop_class {string} Determines if the video should cover or contain within the container.

  Usage:
    {% render 'gallery__video',
      media: product.media,
      ratio_class: 'aspect-ratio-16/9',
      crop_class: 'object-cover'
    %}

  Recommendations:
  - Utilize the 'media' parameter to specify the video to be displayed.
  - Use 'ratio_class' to maintain the aspect ratio according to the design requirements.
{% endcomment %}

{% liquid
  capture container_classes
    echo ratio_class
    echo " w-full h-full"
  endcapture

  if crop_class == "object-cover"
    capture video_class
      echo " object-cover"
    endcapture
  endif
%}

{% render 'component__video', 
  video: media,
  container_class: container_classes,
  video_class: video_class,
  ratio_class: ratio_class,
  enable_custom_controls: false,
  enable_default_controls: true,
  enable_mute_toggle: false,
  enable_autoplay: true,
  enable_loop: true
%}

{% comment %} Badge {% endcomment %}
<div class="absolute z-10 pointer-events-none top-2 left-2">
  {% capture badge_content %}
    {% render 'component__icon', icon: 'play', size: '16' %}
  {% endcapture %}
  {% render 'component__badge', 
    background: 'shade-3',
    text: 'text',
    border: 'transparent',
    icon: '',
    content: badge_content 
  %}
</div>