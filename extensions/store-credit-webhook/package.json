{
  "name": "store-credit-webhook",
  "version": "1.0.0",
  "description": "Shopify webhook to update customer store credits after order completion",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:js",
    "build:js": "mkdir -p dist && cp src/index.js dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  }
}
